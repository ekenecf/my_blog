<div>
<%= link_to "New post", new_user_post_path(@user) %>

  <h1>Posts :</h1>
  <img src="<%= @user.photo %>"  alt="User Profile Picture">
  <h3> <%= @user.bio %> </h3>
  <div >

    <% @recent_posts.each do |post| %>
        <%= render post %>
          <% end %>
          
</div>
<%= link_to "See all posts", user_posts_path(@user)  %>
<%= link_to "See all users", users_path(@user)  %>

<p> nwljd</p>
</div>
